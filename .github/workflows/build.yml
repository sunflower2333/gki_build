name: Build

on:
  push:
    branches: [ master, main ]
    paths:
      - '*.c'
      - 'abi/**'
      - 'docker_exec.sh'
      - 'build.sh'
      - 'package/**'
      - '.github/workflows/build.yml'
  workflow_dispatch:
  workflow_call:
    outputs:
      ver:
        description: "module version from package/module.prop"
        value: ${{ jobs.package.outputs.ver }}

permissions:
  contents: read

jobs:
  build:
    name: build ${{ matrix.tag }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        tag:
          - android12-5.10
          - android13-5.10
          - android13-5.15
          - android14-5.15
          - android14-6.1
          - android15-6.6
          - android16-6.12
    steps:
      - uses: actions/checkout@v4

      - name: Build .ko for ${{ matrix.tag }}
        run: |
          set -e
          OUT="$PWD/package/ko/gh_hugepage_reserve"
          mkdir -p "$OUT"
          OUT23="$PWD/package/ko/gh_ipa23"
          mkdir -p "$OUT23"
          docker run --rm \
            -v "$PWD:/src:ro" \
            -v "$OUT:/out_gh_hugepage_reserve" \
            -v "$OUT23:/out_gh_ipa23" \
            "ghcr.io/ylarod/ddk-min:${{ matrix.tag }}" \
            sh /src/docker_exec.sh gh_hugepage_reserve.c:gh_hugepage_reserve gh_ipa23.c:gh_ipa23

      - name: Upload .ko artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ko-${{ matrix.tag }}
          path: package/ko/gh_hugepage_reserve/${{ matrix.tag }}.ko
          if-no-files-found: ignore

      - name: Upload gh_ipa23 artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ko23-${{ matrix.tag }}
          path: package/ko/gh_ipa23/${{ matrix.tag }}.ko
          if-no-files-found: ignore

  package:
    name: Package zip
    needs: build
    if: ${{ always() && github.event_name != 'pull_request' }}
    runs-on: ubuntu-latest
    outputs:
      ver: ${{ steps.pkg.outputs.ver }}
    steps:
      - uses: actions/checkout@v4

      - name: Download built .ko artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts
          pattern: ko-*
          merge-multiple: false

      - name: Set up QEMU (arm64)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      # The device binaries are NOT committed - they are built fresh here so
      # they always match THIS commit's abi/kapi_abi.tsv + sources (static
      # aarch64 via a QEMU arm64 container; same recipe works locally).
      - name: Build device binaries (kapi_check + balloon, aarch64 static)
        run: |
          set -e
          docker run --rm --platform linux/arm64 -v "$PWD:/src" debian:bookworm \
            bash -c 'apt-get update -qq >/dev/null && \
              apt-get install -y -qq gcc libbpf-dev libelf-dev zlib1g-dev libzstd-dev >/dev/null && \
              gcc -O2 -Wall -I/src/abi /src/abi/kapi_check.c \
                  -lbpf -lelf -lz -lzstd -static -o /src/package/kapi_check && \
              gcc -O2 -Wall -static -o /src/package/balloon /src/tools/balloon.c'
          ls -l package/kapi_check package/balloon

      - name: Assemble package + zip
        id: pkg
        run: |
          set -e
          OUT="package/ko/gh_hugepage_reserve"
          mkdir -p "$OUT"
          # Flatten downloaded artifacts (artifacts/ko-<tag>/<tag>.ko) into the package.
          find artifacts -name '*.ko' -exec cp -v {} "$OUT"/ \;
          echo "=== built modules ==="
          ls -lh "$OUT" || true
          count=$(find "$OUT" -name '*.ko' | wc -l)
          echo "count=$count" >> "$GITHUB_OUTPUT"
          if [ "$count" -eq 0 ]; then
            echo "::error::no .ko built for any kernel tag"
            exit 1
          fi
          ver=$(grep -E '^version=' package/module.prop | cut -d= -f2)
          echo "ver=$ver" >> "$GITHUB_OUTPUT"
          ( cd package && zip -r "../gh-hugepage-reserve-$ver.zip" . )
          ls -lh "gh-hugepage-reserve-$ver.zip"

      - name: Upload package zip artifact
        uses: actions/upload-artifact@v7
        with:
          # archive:false uploads the single file directly (no extra zip wrapper);
          # the artifact name is taken from the file name, so keep it versioned.
          path: gh-hugepage-reserve-${{ steps.pkg.outputs.ver }}.zip
          if-no-files-found: error
          archive: false
